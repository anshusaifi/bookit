# Allow Prisma and bcrypt to run their post-install build scripts.
# pnpm 10+ blocks all package build scripts by default.
# The primary whitelist is in package.json > pnpm.onlyBuiltDependencies.
# This file is copied into Docker before `pnpm install` so it is active
# during the install layer.
enable-pre-post-scripts=true
